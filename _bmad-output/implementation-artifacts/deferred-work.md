# Deferred Work

## Deferred from: code review of story-1.1 (2026-09-08)

- Durcir la gestion d'erreurs de `PWABadge.vue` (fetch/`r.update()` sans `.catch()`, statut HTTP non-200 ignoré, `setInterval` jamais nettoyé au démontage, pas de callback `onRegisterError`, listener `statechange` non retiré après activation) — composant généré tel quel par le starter PWA, hors périmètre de la story 1.1. [1score/src/components/PWABadge.vue] **✅ Supprimé en Story 1.13 (2026-09-10)** : remplacé par `composables/usePwaUpdate.ts` (`registration.update()` sans `fetch` préalable, erreurs absorbées en `console.error('[pwa] …')`, `onRegisterError`, intervalle nettoyé par `onScopeDispose`, application différée à l'accueil).
- Compléter `workbox.globPatterns` (polices `woff`/`woff2`) et fournir un `navigateFallback` de production, une fois le routing implémenté (actuellement seul `devOptions.navigateFallback` est défini et `devOptions.enabled` est `false`). [1score/vite.config.ts] **✅ Traité en Story 1.13 (2026-09-10)** : `globPatterns` étendu à `woff2`, `navigateFallback: 'index.html'` explicite dans `workbox`.
- Résoudre l'incohérence `<html lang="en">` vs. contenu/commentaires en français, une fois le contenu réel de l'application introduit. [1score/index.html] **✅ Traité en Story 1.13 (2026-09-10)** : `<html lang="fr">`, titre « Carom Scoreboard », manifest `lang: 'fr'`.
- Ajouter un outillage lint/format (ESLint/Prettier) pour faire respecter mécaniquement les règles TypeScript strictes déjà activées. [1score/]

## Deferred from: code review of 1-2-redaction-de-claude-md (2026-09-08)

- Formule « élimine le délai 300ms iPad/Android » (AR8) potentiellement obsolète sur navigateurs modernes — héritée telle quelle de la formulation source `architecture.md`, à corriger au niveau de la source. [1score/CLAUDE.md:35]
- Breakpoint `lg:` documenté à ≥1280px ne correspond pas à la valeur par défaut Tailwind (1024px = `lg`, 1280px = `xl` par défaut) et aucun breakpoint explicite ne couvre la plage 768–1280px — hérité tel quel d'AR19 dans `architecture.md`. [1score/CLAUDE.md:112-119]
- L'exemple d'emit `update:score` (convention « kebab-case », AR15) contient un `:` non kebab-case — incohérence héritée de la formulation source. [1score/CLAUDE.md:30]
- Plusieurs règles (AR12 storage, AR17 mutations, AR18 async, AR16 tests, AR8 « élément critique ») sont formulées avec un périmètre strict laissant des zones grises pour les composables, les futurs fichiers `*Service.ts`, ou les tests de store/service — ambiguïtés héritées de la formulation exacte d'`architecture.md`, à traiter lors d'une story future touchant concrètement ces couches. [1score/CLAUDE.md:16-119]
- Aucune convention documentée pour l'identifiant de store Pinia (`defineStore('id', ...)`) ni pour les fichiers utilitaires génériques non préfixés `use` — absents du tableau de nommage source (AR15). [1score/CLAUDE.md:16-31]

## Deferred from: code review of 1-3-selectionner-un-mode-jds-et-demarrer-une-partie (2026-09-08)

- **`status === 'finished'` sans branche de rendu** — `GameView.vue:25-49` ne couvre que `idle` et `playing`, sans `v-else`. Dès qu'un code posera `'finished'`, l'écran sera noir et sans issue (l'ActionBar QUITTER est à l'intérieur de la branche `playing`). Vérifié : `body.innerText` vide. À traiter par la Story 1.10/1.11. **✅ Traité en Story 1.10 (2026-09-10)** : branche `finished` rendue par `GameSummary` + barre à deux CTA (`FIN DE PARTIE`, `UNE PARTIE DE PLUS`).
- **`reprises` en `shallowRef` : les `push` futurs seront non réactifs** — `useGameStore.ts:11`. Trois `computed` de `GameView` dépendent de `reprises.value.length` ; un `shallowRef` ne notifie que sur remplacement de `.value`. Un `reprises.value.push(...)` laissera le compteur REPRISE bloqué sur 1, le bouton ANNULER grisé et l'interversion visible en pleine partie. Indétectable aujourd'hui car seules des affectations existent. AR9 impose `shallowRef` : la convention « toujours réassigner, jamais muter » doit être gravée dans CLAUDE.md avant la Story 1.5/1.8.
- **QUITTER destructif sans confirmation** — `GameView.vue:48` déclenche `resetGame()` sur `@pointerdown`, qui se déclenche au contact et ne peut pas être annulé en glissant le doigt hors du bouton. Note de périmètre déjà présente dans `epics.md:584` — Story 1.15. **✅ Traité en 1.10 (2026-09-10)** : confirmation de sortie « TERMINER LA PARTIE ? » ; la 1.15 livre RECOMMENCER (second picto, remise à zéro confirmée sans récap, 2026-09-10).
- **Branche « catégorie à mode unique » = code mort non testé** — `HomeScreen.vue:38-43`. Les deux seules catégories mono-mode (`3bandes`, `casin`) sont `available: false`, donc bloquées par la garde en amont. Deviendra atteignable en Story 2.1.
- **Robustesse de la normalisation des noms de joueurs** — `HomeScreen.vue:62`. `slice(0,20)` coupe les paires de substitution (un emoji en position 20 devient un surrogate orphelin, chaîne invalide en UTF-8 — impactera la persistance Story 1.12 et la base joueurs Epic 4) ; `toUpperCase()` peut allonger la chaîne (`ß` → `SS`), faisant retronquer et perdre un caractère réel. Découper sur `[...value]` avant la mise en majuscule. *Mise à jour (2026-09-10, Story 1.12)* : **inoffensif pour la persistance** — `JSON.stringify` échappe les surrogates orphelins (`\uD83D`) depuis ES2019 (well-formed stringify), l'aller-retour `localStorage` est sans perte. Reste ouvert pour la troncature/`toUpperCase` elle-même.
- **`Player.id` : champ positionnel redondant** — `types/game.ts:22`. Vaut toujours le nom du champ qui le contient, ce que `swapPlayers` doit restamper manuellement à chaque échange ; risque d'oubli sur les futures mutations (undo, restauration de sauvegarde). **Repris par l'Epic 10 (Story 10.3, 2026-09-11)** : `swapPlayers` est de toute façon scindé en deux actions indépendantes (bille, côté) — bon moment pour éliminer cette redondance.
- **Ordre des classes Tailwind non conforme à CLAUDE.md §7** — plusieurs occurrences (`HomeScreen.vue:97,116`, `CenterPanel.vue:28`). Aucun outillage lint/format n'existe encore dans le projet pour le détecter (déféré depuis la Story 1.1).

## Deferred from: code review of 1-4-configurer-les-parametres-du-match-avant-de-demarrer (2026-09-09)

- **Le plafond de distance est défini deux fois, dans deux unités différentes.** `MAX_DIGITS = 3` dans `PlayerSetupModal.vue:9` et `MAX_TARGET_SCORE = 999` dans `useGameStore.ts:8` ne s'accordent aujourd'hui que par coïncidence numérique. La normalisation est bien placée dans l'action (AR17), mais rien ne fait redescendre la borne vers l'UI : si la règle produit passe à 500, la modale continuera d'accepter 999, la zone joueur affichera 999 (elle lit la valeur brute non normalisée), et `startGame` reclampera silencieusement — la partie démarrerait avec une distance différente de celle affichée une seconde plus tôt. **Repris par l'Epic 10 (Story 10.3, 2026-09-11)** : le pavé numérique est de toute façon retravaillé (moins « fat ») — bon moment pour consolider les deux plafonds en une seule source.
- **Aucune protection contre deux joueurs de même nom.** Nommer les deux joueurs `MICHEL` (ou le joueur 1 `JOUEUR 2`, chaîne saisissable) rend les deux `PlayerPanel` identiques au libellé près ; le seul discriminant restant est la couleur de fond, alors que `swapPlayers()` échange les côtés en cours de partie. Hors périmètre de la Story 1.4 (aucun AC ne le couvre) ; à rattacher à la base joueurs de l'Epic 4 ou à une garde de démarrage.
- **`button:focus-visible` annonce un chemin d'interaction qui n'existe pas.** `main.css:83-86` dessine un contour de 4 px au `Tab`, mais tous les contrôles n'écoutent que `@pointerdown` (AR8, décision assumée en revue de la Story 1.3) : `Entrée`/`Espace` émettent `click` et aucune touche ne réagit. Pré-existant à cette story. À trancher si une cible d'accessibilité clavier / switch access apparaît.
- **Couverture du clavier alphabétique : ni trait d'union, ni apostrophe, ni `Ë Ï Î Ô Û`.** `AlphaKeyboard.vue:14-19` n'offre que l'AZERTY, une rangée de chiffres, `É È À Ç`, `ESPACE` et `⌫`. `JEAN-PIERRE`, `MARIE-CLAUDE`, `D'ARTAGNAN`, `JOËL`, `ANAÏS`, `BENOÎT`, `JÉRÔME` sont donc insaisissables, sans contournement possible puisque le clavier est la seule voie de saisie (aucun champ natif, par décision « borne fixe »). Conforme à l'AC#5 tel qu'écrit, mais l'AC#5 ne couvre pas son propre cas d'usage déclaré. **Reporté sur décision de Nathan : à revoir avec la base joueurs de l'Epic 4**, où les noms seront sélectionnés plutôt que tapés — et où ajouter des touches n'aggravera plus la contrainte de hauteur de la modale. *(2026-09-11 : l'Epic 4 est avancé en V2a et l'identification passe aussi par la **recherche du nom** — ces touches deviennent **dues** avec la Story 4.2.)* **Avancé à l'Epic 10 (Story 10.3, 2026-09-11, `sprint-change-proposal-2026-09-11-refonte-ui.md`)** : `AlphaKeyboard` est de toute façon retouché pour le style contour — complété dès cette epic plutôt que d'attendre la 4.2.

## Deferred from: code review of 1-5-saisir-le-score-dune-serie-au-pave-numerique (2026-09-09)

- **`ÉCHANGER` en pleine reprise laisse le tour au même joueur physique et orpheline la reprise entamée.** `useGameStore.ts` — `swapPlayers` permute les colonnes de `reprises` mais `activePlayer` reste attaché au côté. Après la série du blanc et avant celle du jaune, un échange redonne la main à celui qui vient de jouer ; sa série suivante ouvre une nouvelle ligne, la ligne `{null, n}` ne se complète jamais et `completedReprises` (donc le compteur REPRISE) accuse une unité de retard jusqu'à la fin de la partie. **Décision de Nathan (2026-09-09) : accepté et documenté** (Décision 15 de la story), le tour ne suit pas le joueur et l'échange n'est pas bloqué ; le rattrapage passera par `ANNULER` (Story 1.8). À réévaluer si le cas se produit en usage réel. *Précision (Story 1.7, 2026-09-09)* : l'échange lui-même **n'est pas annulable** (décision produit, `ÉCHANGER` est son propre inverse) ; le rattrapage consiste à **annuler jusqu'avant la série orpheline**, puis à la ressaisir du bon côté.

- **Sortie destructive sur `pointerdown`, et qui change de colonne à chaque tour.** `GameView.vue:113,145` — le picto de sortie appelle `resetGame()` au contact, à l'emplacement où se trouvait `AJOUTER LES POINTS` au tour précédent. Confirmation prévue Story 1.15 ; l'alternance est la décision produit AC4 de la 1.5. **✅ Traité en Story 1.10 (2026-09-10)** : le picto ouvre une pop-up de confirmation « TERMINER LA PARTIE ? » (`PromptModal`, CTA `VOIR LE RÉCAP` / `ANNULER`) menant au récap — plus rien de destructif au contact ; sans aucune série, retour direct à l'accueil (rien à perdre). L'alternance de colonne demeure.
- **Panneau joueur entier en `role="button"` englobant deux vrais `<button>`.** `PlayerPanel.vue:100-105` — contenu interactif imbriqué interdit par ARIA, pas de `tabindex` ni d'`aria-disabled` quand le panneau est inerte ; un appui de paume sur la carte adverse enregistre une série de 0. Geste décidé par AC14 ; accessibilité clavier hors cible (revue 1.4).
- **Pop-up de saisie sans sémantique de dialogue ni `prefers-reduced-motion`.** `ScoreEntryModal.vue:120-130`, `main.css:52-79` — pas de `role="dialog"`/`aria-modal`, pas de gestion du focus, trois animations sans garde reduced-motion. Pré-existant sur `PlayerSetupModal`, même arbitrage borne fixe. **Repris par l'Epic 10 (Story 10.7, 2026-09-11)**, avec la même dette sur `PromptModal` ci-dessous — toutes les pop-ups sont retouchées pour le style contour.
- **Voile de pop-up avec handlers pointer sans `role="button"` (CLAUDE.md §2).** `ScoreEntryModal.vue:120` — la règle mérite une exception explicite pour les voiles plutôt qu'un rôle ARIA erroné sur un fond plein écran. *(`PlayerSetupModal.vue` retiré de la liste : composant supprimé en Story 10.3.)*
- **Markup dupliqué dans la barre basse.** `GameView.vue:106-176` — CTA et SVG de sortie copiés pour chaque colonne, alignement dépendant du `px-4` d'`ActionBar` via `-mx-4`/`ml-4`/`mr-4` : changer le padding de la barre désaligne silencieusement les colonnes. Nettoyage sans impact fonctionnel. *Mise à jour (2026-09-10, Story 1.15)* : **aggravé** — le picto RECOMMENCER et son SVG sont dupliqués à leur tour dans chaque colonne (`<template v-else>` avec deux pictos), toujours à nettoyer. **Repris par l'Epic 10 (Story 10.4, 2026-09-11)** : la barre basse est reconstruite en CTA picto + libellé (`ANNULER` y rejoint `QUITTER`/`RECOMMENCER`/`PARAMÈTRES`) — l'occasion d'éliminer la duplication plutôt que de l'étendre encore.
- **`MAX_TARGET_SCORE = 999` et `MAX_SCORE_DIGITS = 3` encodent FR7 indépendamment.** `useGameStore.ts:8-12` — s'ajoute au doublon déjà différé en revue 1.4 (`MAX_DIGITS` de `PlayerSetupModal`). Réconciliation hors périmètre par décision de la story 1.5.

## Deferred from: code review of 1-7-annuler-la-saisie-en-cours-avant-validation (2026-09-09)

- **`undoLastAction()` par pilotage déporté pendant la pop-up ouverte écrase le buffer en cours de frappe.** `useGameStore.ts:340-357` — l'undo restaure `currentInput`/`isNegative` sans garde ; la modale afficherait vide et l'auto-validation tomberait sur un buffer vide. Injoignable depuis l'UI (le voile plein écran recouvre `ANNULER`). À traiter avec le pilotage V2+ : `GameView` doit fermer la pop-up avant d'appeler l'undo.
- **`isNegative` n'est pas remis à zéro avant le snapshot, contrairement à `currentInput`.** `useGameStore.ts:317-318` — sans effet tant que rien ne pose le drapeau ; la Story 1.9 (saisie négative) devra vider le signe avant `pushHistory()` sous peine de le voir réapparaître après un undo (même piège que le `53` du Debug Log 1.7). *Mise à jour (2026-09-10)* : la **Story 1.9 est annulée** (FR10 couvert par les CTA `−`/`+`) ; `isNegative` reste donc inerte à `false`, sans risque. Le champ est un candidat à la **suppression** du modèle (`GameState`, `GameSnapshot`, `mirrorSnapshot`, `swapPlayers`) lors de la Story 1.12, qui fixera le format persisté. **✅ Supprimé en Story 1.12 (2026-09-10)** : `isNegative` retiré de `GameState`, `GameSnapshot`, `mirrorSnapshot`, du store et des tests.
- **Undo refusé hors `playing` : une série gagnante mal saisie serait irrattrapable une fois `status = 'finished'`.** `useGameStore.ts:341` — AC7 de la 1.7 exige le no-op hors partie et `finished` n'existe pas encore. À trancher en Story 1.11 (autoriser l'undo depuis `finished` et rouvrir la partie, ou confirmer la fin). **✅ Tranché en Story 1.10 (2026-09-10)** : le récap est **terminal**, `undoLastAction` reste un no-op hors `playing` (désormais testé avec un état `finished` réel). La correction passe par la **croix** des pop-ups « PARTIE TERMINÉE » et « TERMINER LA PARTIE ? », qui ramènent au scoreboard sans clore la partie ; la détection rejoue à la validation suivante. *Revue au rendu (Nathan, 2026-09-10)* : la croix de « PARTIE TERMINÉE » est **retirée** — on ne revient pas au scoreboard une fois la fin détectée, une série gagnante mal saisie n'est plus rattrapable, **accepté**. La pop-up de sortie n'a pas de croix non plus : son retour est un CTA `ANNULER`.
- **Le snapshot porte `name`/`targetScore` : une édition de joueur en partie serait défaite par l'undo.** `useGameStore.ts:118-127,349-350` — le commentaire d'invariante ne couvre que la mutation en place ; un REMPLACEMENT de l'objet joueur (renommage, Story 1.14) serait lui aussi annulé. À traiter en 1.14 : ne restaurer que le score, ou ré-appliquer l'édition après restauration.
- **Deux pointeurs simultanés sur `ANNULER` = deux annulations.** `CenterPanel.vue:32` — `@pointerdown` n'ignore pas les pointeurs secondaires. Cas rare ; garde `isPrimary` à centraliser dans `usePointerEvents.ts` (CLAUDE.md §2) quand ce composable existera.

## Deferred from: code review of 1-10-terminer-une-partie-et-consulter-le-recapitulatif-automatique (2026-09-10)

- **Store non durci contre les actions reçues pendant une pop-up de fin ouverte (pilotage déporté V2+).** `useGameStore.ts:399-408,444-461` — `checkEndOfGame` écrase un `endPrompt` existant ; `undoLastAction`/`adjustScore`/`swapPlayers` ne ferment pas la pop-up ; après `dismissEndPrompt` en égalisatrice, le blanc peut revalider (série plafonnée à 0, `hasReachedTarget` vrai) et déclencher une seconde offre ; `finishGame` appelé par la sortie pendant une offre vaut refus de l'égalisatrice (blanc gagnant) au lieu du prorata. Injoignable au doigt : le voile plein écran recouvre tout et `dismissEndPrompt` n'a aucun bouton. Même famille que « undo par pilotage déporté pendant la pop-up » (revue 1.7) : à traiter avec le pilotage V2+, en fermant/gelant les actions tant qu'une décision est attendue.
- **`endPrompt` absent de `GameState` alors que le store promet sa persistance en 1.12.** `types/game.ts:98-113`, `useGameStore.ts:115-118` — `GameState` reçoit `winner`/`finishedAt`/`equalizingReprise` mais pas la pop-up en attente ; une restauration en pleine offre d'égalisatrice retomberait sur un scoreboard sans pop-up, le blanc à sa distance et `equalizingReprise` faux. À fixer avec le format persisté (Story 1.12). **✅ Traité en Story 1.12 (2026-09-10)** : `endPrompt` (et `entryOpen`, `history`, `scoreAdjustments`, `sidesSwapped`) font partie de `GameState`, persistés et restaurés — testé sur l'offre d'égalisatrice et « PARTIE TERMINÉE ».
- **`PromptModal` sans sémantique de dialogue.** `PromptModal.vue:38-46` — pas de `role="dialog"`/`aria-modal`/`aria-labelledby` vers le `h2`, pas de gestion du focus. Même dette et même arbitrage « borne fixe » que `ScoreEntryModal`/`PlayerSetupModal` (revue 1.5) ; à traiter d'un bloc le jour où l'accessibilité clavier entre en cible. **Repris par l'Epic 10 (Story 10.7, 2026-09-11)**, avec `ScoreEntryModal`/`PlayerSetupModal` ci-dessus.
- **Bandeau du récap : un nom de 20 lettres larges masque la distance.** `GameSummary.vue` (`summary-player1`/`summary-player2`, `truncate`) — « WWWWWWWWWWWWWWWWWWWW / 8 » est coupé par l'ellipse avant « / 8 » dans les deux formats tablette. Cas extrême (20 caractères les plus larges) relevé en passe visuelle de la revue 1.10 ; à traiter le jour où le bandeau est retouché (distance dans un `span` séparé non tronqué, ou nom sur deux lignes). **Repris par l'Epic 10 (Story 10.5, 2026-09-11)** : le récap est retouché pour ajouter la sidebar — l'occasion de corriger cette troncature au passage.

## Deferred from: code review of 1-12-preserver-letat-de-la-partie-en-cas-de-fermeture-accidentelle (2026-09-10)

- **Garde structurelle de `loadGameState` superficielle.** `storageService.ts:31-47` — `isPersistedGame` vérifie la version, le statut et la présence/forme des conteneurs, mais pas les valeurs internes : `mode` hors catalogue, `activePlayer` invalide, `name`/`score`/`targetScore` absents ou non numériques, cellules de `reprises` nulles, éléments de `history` malformés, `currentInput` non chaînes, `scoreAdjustments` non finis, `endPrompt`/`winner` de mauvais type. Une telle sauvegarde passe la garde et fait planter la vue au premier rendu ou au premier undo — hors du service, à rebours d'AR12. Injoignable sans manipuler le storage à la main (le store est l'unique écrivain). Décision de cadrage de la story (garde minimale, pas de Zod, filet de sécurité à développer vite). À revoir si un jour le format est lu par un autre écrivain (import, multi-appareils, Epic 3).
- **Coût par action de la pile persistée.** `useGameStore.ts:35,621` — chaque action, frappe au pavé comprise, sérialise l'intégralité de `history` (chaque snapshot recopie le tableau des reprises : ≈ 350 Ko pour 100 actions × 60 reprises), et `checkSavedGame` reparse ce blob de façon synchrone avant le premier rendu. Risque consigné en commentaire sur `MAX_UNDO_DEPTH` ; un bornage à l'écriture (`history.slice(-N)` dans `persistedState`) tient en une ligne, à poser avec le `+1` par point du 3 Bandes (Epic 2).

## Deferred from: dev-story 1-13-fonctionner-offline-et-sinstaller-comme-application-native (2026-09-10)

- **AC5 non vérifié — actions manuelles de Nathan** : rattachement du dépôt GitHub au site Netlify (AR11), puis installation et test hors ligne sur iPad et Android, et observation d'une mise à jour automatique réelle. Check-list détaillée dans [1-13-actions-manuelles-nathan.md](1-13-actions-manuelles-nathan.md). La story ne passe pas en `done` avant. **✅ Traité (2026-09-10)** : testé à la main par Nathan, story 1.13 passée en `done`.

## Deferred from: code review of 1-15-reinitialiser-une-partie-en-cours (2026-09-10)

- **État local des pop-ups de confirmation non réconcilié avec le store (pilotage déporté V2+).** `GameView.vue:137-193` — `exitPromptOpen` (1.10) et `restartPromptOpen` (1.15) sont des `ref` locaux que rien ne referme quand `status` quitte `playing` ou qu'une partie neuve démarre (`restartGame`/`rematch`/`resetGame` reçus du store) : « TERMINER LA PARTIE ? » ou « RECOMMENCER LA PARTIE ? » resterait affichée sur le scoreboard neuf. Pas d'exclusion mutuelle entre les deux pop-ups (`askRestart` ignore `exitPromptOpen` et réciproquement) ; `confirmRestart` ne revérifie pas `canUndo` (retombé à faux par un `undoLastAction` déporté pendant la pop-up) ; un `restartGame()` reçu pendant `entryOpen` referme le pavé sans `lockPanels()`. Injoignable au doigt : le voile plein écran recouvre la barre. Même famille que « store non durci contre les actions reçues pendant une pop-up » (revues 1.7, 1.10) : à traiter d'un bloc avec le pilotage V2+ (un `watch(status)` qui referme les confirmations locales, ou une seule `ref` « quelle pop-up est ouverte »).

## Deferred from: bmad-create-story 2-1-selectionner-le-mode-3-bandes-et-demarrer-une-partie-avec-chronometre-actif (2026-09-10)

- **Pause/reprise du chronomètre — retirée du périmètre V1b** (décision de Nathan, 2026-09-10). L'AC de l'epic (« fondation pour l'arbitre déporté V2 ») n'est pas livré : la pause n'a de sens qu'en compétition arbitrée, à l'entraînement un chrono qui continue pendant une interruption est sans conséquence. `useTimer.ts` n'expose ni `pauseTimer`, ni `resumeTimer`, ni `isPaused`. À reprendre dans une future épic Compétition/Arbitrage (proche d'Epic 7) si le besoin se confirme.
- **Trou de planification « nombre de sets » (FR15).** `epics.md` signale depuis la Story 1.4 que la configuration du nombre de sets doit trouver « son propre point d'entrée » dans l'Epic 2, mais aucune des trois stories (2.1, 2.2, 2.3) ne le couvre ; aucune maquette UX n'existe, l'architecture n'en dit rien. À planifier comme story dédiée (réglage de match, pas de joueur — donc hors `PlayerSetupModal`).
- **Non-persistance du chrono.** `useTimer.ts` ne fait pas partie de `GameState` (AR14) : un rechargement en pleine partie 3 Bandes (tablette qui recharge après une mise à jour PWA, Story 1.13/1.12) restaure la partie mais fait repartir le chrono à 40 s. Si c'est gênant à l'usage, ajouter le chrono à `GameState` est une extension mineure qui incrémente `GAME_STORAGE_VERSION`.

## Deferred from: dev-story 2-2-incrementer-le-score-point-par-point-tap-du-joueur-assis (2026-09-11)

- **Compteur `POUR n` non livré.** La note de périmètre de la Story 2.2 (`epics.md`, héritée de la 1.6) prévoit `POUR 3` / `POUR 2` / `POUR 1` sous le score en 3 Bandes, « à confirmer à la création de la story ». Nathan ne l'a pas cité en donnant les directives de la 2.2 (2026-09-11) : non implémenté, à confirmer avant de l'ajouter (une story courte, `PlayerPanel` seul, calcul `distance − score`). **✅ Livré en Story 2.4 (2026-09-11)** à la demande de Nathan — prop `showRemaining` de `PlayerPanel`, seuil 3.
- **Le pavé de secours du 3 Bandes n'a plus de point d'entrée.** `GameView.vue` — en 3 Bandes le CTA de la barre basse est `+1 POINT` ; `ScoreEntryModal` reste dans le code mais rien ne l'ouvre dans ce mode. La Story 2.3 (saisie globale de secours) doit définir son geste (appui long sur `+1 POINT` ? picto dédié ?) et la règle de non-double-comptage avec la série déjà comptée au tap (`openSeriesValue`). **✅ Clos (2026-09-11)** : Story 2.3 **annulée** par Nathan — pas de pavé de secours au 3 Bandes, le `+1` suffit ; l'absence de point d'entrée est le comportement voulu.
- **`ANNULER` ne relance pas le chrono.** `GameView.vue` — annuler un tap ou une main rendue restaure le score et le tour, mais le chrono continue là où il en est (Nathan n'a cité que le `+1` et la main rendue). Si l'usage montre qu'il faut repartir à 40 après un `ANNULER`, c'est un appel à `resetTimer()` dans `undoLastAction` de la vue.
- **Anneau petit sur un écran bas.** `ShotClock.vue` / `CenterPanel.vue` — à 1180×673 (iPad paysage avec barre Safari), l'anneau retombe à ~104 px : la colonne cède d'abord la place à REP, aux deux boutons de 90 px et aux trois `gap-4` (32 px). Lisible, mais si Nathan le trouve trop petit, réduire `gap-4` en `gap-2` sur la colonne ou compacter REP. *Nathan (2026-09-11) : « pour l'instant ça me paraît ok »* — sans le libellé CHRONO, l'anneau gagne d'ailleurs ~32 px sur ce format.
- **Rembobinage visible au RECOMMENCER.** `ShotClock.vue` — la transition d'une seconde sur `stroke-dashoffset` fait se remplir l'anneau de vide à plein au reset (déjà noté en 2.1, toujours vrai avec la grâce de 2 s qui le rend plus visible). Coupure de la transition le temps d'un reset si Nathan le juge gênant.

## Deferred from: code review of 2-1 + 2-2 + 2-4 (2026-09-11)

- **Décompte non ancré sur l'horloge.** `useTimer.ts:783-786` — `setInterval(tick, 1000)` cumule la dérive et se fige en onglet caché ou en veille ; 40 ticks ≠ 40 secondes. Un `deadline = Date.now() + secondsRemaining × 1000` recalculé à chaque tick (`Math.ceil((deadline − now) / 1000)`) serait exact au retour au premier plan. Sans conséquence tant que la tablette reste allumée et au premier plan (usage cible).
- **`incrementSeries`/`passTurn` acceptés alors que `endPrompt` est posé.** `useGameStore.ts:442` — un pilotage déporté (V2+) pourrait créditer des points et empiler des snapshots derrière la pop-up de fin sans que `endPrompt` soit recalculé. Injoignable au doigt (voile plein écran). Même famille que `validateScoreInput` : à durcir d'un bloc avec le pilotage V2+.
- **Un snapshot complet des reprises par tap.** `useGameStore.ts:172-174,680` — en 3 Bandes chaque `+1` empile un snapshot qui porte tout le tableau `reprises`, et `persistedState` sérialise la pile entière à chaque tap (écriture localStorage quadratique, bornée par `MAX_UNDO_DEPTH = 1000`). De l'ordre de quelques centaines de Ko pour une partie longue : acceptable, à surveiller si des `QuotaExceededError` apparaissent.
- **ÉCHANGER pendant une reprise entamée casse la déduction de la série ouverte (2.2).** `useGameStore.ts:367-372` — `openSeriesValue` suppose « le blanc ouvre », mais `swapPlayers` met les reprises en miroir en laissant le tour attaché au côté. Reproduit : 3 Bandes, blanc `+1` ×2 → ÉCHANGER → `+1` → main rendue → `[[1,2],[0,null]]` (reprise fantôme à 0 par-dessus la série tapée) ; JDS, blanc valide 5 → ÉCHANGER → main rendue → aucun 0 enregistré (changement de comportement JDS). Pistes : refuser ÉCHANGER tant que la dernière reprise est à moitié remplie (un mode ou les deux), ou stocker explicitement la série ouverte dans `GameState`. *Nathan (2026-09-11)* : « je vais retravailler ce mécanisme d'échange plus tard, je ne suis pas très au clair sur les décisions à prendre » — **backlog**, à reprendre comme story dédiée (ÉCHANGER : quand, et quel effet sur le tour et la reprise en cours). **✅ Clos le 2026-09-11 (Epic 10, Story 10.3 — avancée depuis la 10.4 à la passe epics, `sprint-change-proposal-2026-09-11-refonte-ui.md`)** : `ÉCHANGER` est **retiré du jeu en cours de partie** (refonte UI/UX Premium) — le mécanisme incriminé n'existe plus, sans objet à corriger. L'interversion pré-partie (paramétrage joueurs) devient deux actions indépendantes (`CHANGER DE BILLE`/`CHANGER DE CÔTÉ`), sans le bug de déduction de série ouverte puisqu'aucune partie n'est en cours à ce moment.

## Deferred from: bmad-correct-course 2026-09-11 (parties rattachées au profil joueur)

- **Story 3.1 recadrée, non développée.** `3-1-…md` repasse en `backlog` : son socle (Dexie 4, `GameRecord` par côté avec distance/vainqueur/type de fin/corrections/stats dénormalisées, déclencheur `finishGame`, objets plain contre `DataCloneError`, `fake-indexeddb` en `setupFiles`, piège des fake timers) reste la base de la future **file locale de synchronisation** ; à compléter par un identifiant joueur par côté (`null` = invité) et un statut de sync, après l'Epic 4. Ne pas développer avant : cela éviterait une migration de schéma Dexie. La question « quitter sans récap » (report 1.15) y est tranchée : aucun nouveau chemin, une fin manuelle est une partie terminée marquée `manual`.
- **Décision d'architecture backend due** avant la Story 4.0 (`architecture.md` › « Backend, Authentification & Synchronisation (V2a) — à décider ») : plateforme EU, auth ID court, recherche par nom, modèle joueur/partie, sync, RGPD, couche réseau, secrets Netlify. `CLAUDE.md` à compléter ensuite (couche réseau, erreurs réseau, environnement) — rien à documenter tant que la plateforme n'est pas choisie.
- **Réécriture des stories 3.1-3.5 et découpage de l'Epic 4** (4.0 ajoutée, 4.1/4.2 amendées, 3.5 fusionnée dans 4.3/4.4, 5.2 déplacée vers l'Epic 3) par une passe `create-epics-and-stories` puis `sprint-planning`, une fois l'architecture décidée. Les clés `3-2`…`3-5` et `5-2` de `sprint-status.yaml` sont conservées annotées jusque-là.
- **Consolidation V1 jeu avant le chantier profil** (décision de Nathan : « des choses plus prio pour une V1 clean ») — candidats déjà consignés, à arbitrer : ~~ÉCHANGER mi-reprise (backlog)~~ **clos le 2026-09-11, voir ci-dessous**, prolongation en cas d'égalité (story à créer, 1.10), nombre de sets FR15 (trancher ou abandonner), chrono non ancré sur l'horloge et non persisté, veille de la tablette côté accueil, outillage lint ; et le critère PRD encore ouvert : **un club pilote en usage réel**.
- **Deux joueurs de même nom** (revue 1.3) et **renommage en partie** (FR39, 1.14 annulée) : à reprendre sous la forme « réassocier un joueur identifié » avec l'Epic 4.

## Deferred from: bmad-correct-course 2026-09-11 (refonte UI/UX Premium, Epic 10)

- **Nouvelle Epic 10 — Refonte UI/UX Premium (1Score)**, insérée après l'Epic 2, avant l'Epic 4, à partir du brief écran par écran de Nathan (`explore/basic-ui-brainstorming-2026-09-11.md`). Découpage en 7 stories (10.1-10.7, voir `epics.md`). PRD, architecture et `sprint-status.yaml` annotés le jour même ; détail complet renvoyé à `bmad-create-ux-design` puis `bmad-create-epics-and-stories`.
- **`ÉCHANGER` clos** : retiré du jeu en cours de partie (Story 10.3, le modèle joueur changeant dans cette story — passe epics du 2026-09-11) — voir l'entrée « ÉCHANGER pendant une reprise entamée » ci-dessus, désormais marquée résolue.
- **Deux évolutions de mécanique au-delà de l'habillage** : passage de tour par CTA dédiée `PASSER LE TOUR` (remplace le tap sur la carte adverse) ; interversion bille/côté scindée en deux actions indépendantes au paramétrage (`swapPlayers` à réécrire). Aucun changement de `GameState` ni de règle de calcul de score.
- **Risque technique ouvert** : faisabilité de « Fermer l'application » (sidebar Accueil) — pas d'API standard de fermeture d'une PWA installée, spike requis avant la Story 10.1. **Reporté hors Epic 10 (Nathan, passe epics du 2026-09-11) : non prioritaire — l'item est affiché inerte (BIENTÔT) en 10.1, spike et comportement réel à reprendre dans une story ultérieure.**
- **Dette UI/UX cumulée dans l'Epic 10** (Story 10.7, sauf mentions contraires ci-dessus) : ~~plafonds du pavé numérique dupliqués~~ (DT1, **✅ clos en 10.3** — `MAX_TARGET_SCORE` exporté, `MAX_DIGITS` disparu avec `PlayerSetupModal`), markup dupliqué de la barre basse, pop-ups sans sémantique de dialogue ni garde `prefers-reduced-motion`, ~~clavier alphabétique incomplet~~ (DT4, **✅ clos en 10.3** — tiret, apostrophe, `Ë Ï Î Ô Û`), ~~troncature du nom au récap~~ (DT6, **✅ clos en 10.5** — nom et distance sont deux éléments, `min-w-0 truncate` / `shrink-0` séparés d'un filet ; vérifié au navigateur avec un nom de 20 `W` aux trois formats), ~~`Player.id` redondant~~ (DT5, **✅ clos en 10.3** — champ supprimé, la bille est la seule identité).

## Deferred from: renommage complet en 1score (2026-09-11)

Dossier applicatif, clé de sauvegarde et chemins des documents sont renommés. Restent, jugés pénibles et remis à plus tard (Nathan) :

- **Dépôt GitHub `nlegendree/carom_scoreboard` → `1score`** — Settings › Repository name (GitHub redirige l'ancienne adresse), puis `git remote set-url origin git@github.com:nlegendree/1score.git` ; vérifier que le site Netlify reste rattaché au dépôt et redéploie au push suivant.
- **Dossier local `carom_scoreboard` → `1score`** — à faire hors session Claude Code. Déplacer aussi la mémoire du projet, `~/.claude/projects/-Users-nathan-Developer-04-Projets-carom-scoreboard/` → `-Users-nathan-Developer-04-Projets-1score/`, sinon les sessions suivantes repartent sans mémoire. Au passage : corriger le chemin périmé (`Documents/…`) de `.claude/settings.local.json` et supprimer le vieux `.claude/projects/-Users-nathan-Documents-04-Projets-carom-scoreboard/` suivi par Git.
- **Site Netlify** — le renommer change l'adresse `*.netlify.app`, donc l'origine : partie sauvegardée perdue et PWA à réinstaller sur chaque tablette. Sans conséquence tant qu'aucune tablette n'est en usage réel.
- **À vérifier au prochain déploiement** : `netlify.toml` pointe désormais sur `base = "1score"`. Si le champ *Base directory* a été saisi à la main dans l'interface Netlify (`carom-scoreboard`), le build échouera : le vider ou le passer à `1score`.

## Deferred from: dev-story 10-1-refonte-de-laccueil-barre-laterale-tuiles-de-mode-fond-degrade (2026-09-11)

- **L'effet d'appui des tuiles se voit à peine au doigt.** La navigation part au `pointerdown` (AR8, NFR1) : l'accueil disparaît dès le contact, et `active:brightness-125` ne dure qu'un instant. Seul l'éclaircissement au survol (souris) se voit vraiment. Pour le rendre visible, il faudrait naviguer au `pointerup` ou après un court délai. C'est un arbitrage produit, contraire à la règle du geste immédiat.
- **Tailles plafonnées trop bas pour l'écran 21,5″** (1920×1080, cible d'installation à terme). `text-picto` (12 px), `text-tile-title` (36 px) et `text-hero` (56 px) atteignent leur plafond dès la tablette, alors que les tuiles font 450 px de large. Il faudra relever les plafonds des `clamp()`, ou un palier `lg:`, quand l'écran arrivera.
- **Fond d'accueil** : Nathan envisage de retravailler le dégradé gris vers noir pour le rendre « plus engageant ».
- **Documents encore écrits pour le portrait**. Seules les notes datées de la passe de rendu annulent le portrait ; le texte d'origine n'a pas été réécrit ligne à ligne :
  - `epics.md` (AC des stories 10.3 à 10.5 et 10.7) ;
  - spec UX §10 (UX-DR29, UX-DR50) ;
  - CLAUDE.md §8, dont le palier par défaut dit encore « smartphone / portrait ».
  
  Les stories 10.2 à 10.7 doivent partir des notes datées ; nettoyer le texte à leur création.
- **Revue de code de la 10.1 reportée** (décision de Nathan, 2026-09-11 : « j'ai envie d'avancer là, on fera tous les fix à la fin »). La story est committée et laissée en `review` : **livrée mais non relue**. Les revues de l'Epic 10 seront passées groupées en fin d'epic, idéalement avec un autre modèle, et les correctifs appliqués à ce moment-là.

## Deferred from: dev-story 10-2-refonte-de-la-selection-des-modes-jds-tuiles-et-choix-cadre (2026-09-12)

- ~~**Voile et flou des pop-ups à revoir avec le pavé numérique**~~ **✅ Clos (Story 10.3, 2026-09-12)** : décision de Nathan à la création de la story — « il faut qu'on puisse lire les champs de nom et score qui vont se remplir en arrière-plan ». Les hôtes de saisie du paramétrage (`NumericPadDock`, `AlphaKeyboardSheet`) ne posent **aucun voile ni flou** : le dock remplace les CTA dans la colonne centrale, le bandeau est monté dans le flux sous la zone principale, les deux cartes restent nettes et entières. Le voile `bg-black/60` + flou 8 px reste celui des **pop-ups de décision** (`PromptModal`, `ScoreEntryModal`), inchangé — la question ne se pose plus, les deux familles sont désormais distinctes.
- **`ScoreEntryModal` et `PlayerSetupModal` n'ont pas suivi le nouveau style des pop-ups.** `PromptModal` est passé aux angles vifs (`--radius-cta`, carte à `--radius-modal`), au dégradé par CTA (`--gradient-blue` / `--gradient-neutral`) et au flou 8 px ; les deux autres gardent leurs `rounded-3xl`/`rounded-2xl`, `bg-accent` et leur flou 12 px. À aligner en **Story 10.7** (finition transverse), ou plus tôt si la 10.3 les refond — elle supprime `PlayerSetupModal` au profit d'une saisie en place.
- **Contraste du blanc sur `--gradient-blue` : 3,46:1** au point le plus clair du dégradé. Conforme AA pour du **texte large**, ce que sont les libellés de CTA (~24 px en `font-black`), mais sans marge. À revalider dans la passe contraste AA de la **Story 10.7**, en même temps que les autres écrans.
- **Revue de code de la 10.2 reportée**, comme la 10.1 : livrée, committée, laissée en `review` sans relecture (décision de Nathan, 2026-09-11 — revues et correctifs groupés en fin d'Epic 10).

## Deferred from: dev-story 10-3-refonte-du-parametrage-joueurs-saisie-en-place-bille-et-cote-dissocies (2026-09-12)

- ~~**Le médaillon du paramétrage s'aplatit en ellipse**~~ et ~~**le bandeau de nom occupe ≈ 56 % de la hauteur**~~ — **✅ sans objet (2e passe de rendu, 2026-09-12)** : le médaillon est supprimé et les claviers sont devenus des pop-ups, donc la page ne reflue plus autour d'eux. Les deux arbitrages de hauteur disparaissent avec la mise en page qui les créait.
- **Les noms `NumericPadDock` et `AlphaKeyboardSheet` ne décrivent plus ce qu'ils sont.** Depuis la 2e passe de rendu, ni « dock » (logé dans une colonne) ni « sheet » (bandeau en flux) : ce sont deux pop-ups. Les renommer touche six fichiers plus les références dans `epics.md`, la spec UX et `architecture.md` — à faire d'un bloc à la **revue de fin d'Epic 10**, pas au milieu d'une passe de rendu.
- **Le picto des deux réglages est empilé au-dessus du libellé, pas en ligne comme la référence Cueuny.** `HomeScreen.vue` — la colonne centrale fait 1/5 de la zone, soit **73 px par bouton** à 1133×744 : un picto de 24 px suivi de « CHANGER DE BILLE » déborde du bouton (vu à la passe navigateur). À rebasculer en ligne si la colonne s'élargit un jour — ce que la 10.4 pourrait décider en reprenant le scoreboard.
- **`ScoreEntryModal` garde l'ancien style de pop-up.** Inchangé par cette story (périmètre : l'étape `players`) ; son alignement sur le nouveau style est déjà noté pour la **Story 10.7**, avec la passe contraste AA. À noter qu'il embarque `NumericPad`, dont les touches ont changé de style ici : la pop-up mélange donc des touches à angles vifs et une carte arrondie jusqu'à la 10.7.

## Deferred from: 4e passe de rendu de la story 10.3 (2026-09-12)

- ~~**Les pictos de bille des en-têtes de carte sont des disques CSS provisoires.**~~ **✅ Clos (2026-09-12)** : Nathan a fourni `public/bille_blanche.png` et `public/bille_jaune.png` (512×512), servis depuis `public/` comme `logo.png` — aucune ressource réseau.
- **`--radius-key` est une seconde exception aux angles vifs.** `main.css` — après `--radius-modal` (carte de pop-up), les touches de clavier prennent 8 px de rayon (réf. Cueuny, décision de Nathan du 2026-09-12). La direction « angles vifs » de l'epic vaut donc pour les panneaux et les CTA, plus pour tout. À consigner tel quel dans CLAUDE.md §7 à la Story 10.7.
- **`ScoreEntryModal` hérite du nouveau style de touches sans avoir été revu.** `keyClasses.ts` est partagé : la pop-up de saisie de série du scoreboard a donc des touches en relief façon Cueuny dans une carte restée à l'ancien style. L'alignement complet est déjà prévu en **Story 10.7**.

## Deferred from: 5e passe de rendu de la story 10.3 (2026-09-12)

- **La géométrie de l'étape `players` est dupliquée dans deux tokens CSS.** `main.css` — `--setup-popup-inset-left` / `--setup-popup-inset-right` recalculent, en `calc()`, la largeur de la barre latérale, le padding de la zone principale, la colonne centrale à 1/4 et les gouttières, pour centrer les pop-ups de saisie dans la zone que la carte visée laisse libre. **À faire bouger avec la mise en page** : rien ne les relie au gabarit, et une pop-up mal recentrée est la première chose qui se verra. Une vraie solution (mesurer la carte et poser une variable au runtime, ou passer par `@container`) est disproportionnée pour un écran ; à revoir si la 10.4 introduit le même besoin sur le scoreboard.
- **`PromptModal.dismissible` est opt-in et doit le rester.** `PromptModal.vue` — les pop-ups de FIN DE PARTIE gardent un voile inerte parce qu'elles montent SOUS le doigt qui vient de valider une série (AC18, Décision 12). Toute tentative future de « généraliser le tap dehors » doit buter sur ce cas-là.
- **Le voile de toutes les pop-ups est passé sans flou** (`bg-black/25`), alignement demandé par Nathan sur les pop-ups de saisie. Conséquence non vérifiée : la lisibilité des pop-ups de fin de partie par-dessus un scoreboard chargé. À regarder à la passe contraste de la **Story 10.7**.

## Deferred from: dev-story 10-4-refonte-du-scoreboard-carte-joueur-passer-le-tour-dock-de-saisie-barre-basse-en-pictos (2026-09-14)

- ~~**DT2 — markup dupliqué de la barre basse**~~ **✅ Clos (Story 10.4)** : `ActionBar` est réécrite en barre basse de scoreboard, **un seul markup** pour les deux côtés, l'ordre venant de `flex-row` / `flex-row-reverse` et les quatre pictos d'un `v-for` sur une liste unique. Les copies de CTA et de SVG par colonne disparaissent, ainsi que les marges négatives `-mx-4` / `ml-4` / `mr-4` qui compensaient le padding de la barre : elle reproduit désormais la grille de la zone de jeu (2/5 · 1/5 · 2/5) au lieu de la rattraper.
- ~~**`role="button"` englobant de `PlayerPanel`** (dette de la revue 1.5)~~ **✅ Clos de fait (Story 10.4, AC4)** : la carte n'est plus tapable, l'emit `pass-turn`, le handler de racine, la garde `if (props.active) return` et le rôle sont retirés. `touch-manipulation select-none` restent en classes Tailwind — le CSS global ne cible que `button, [role="button"]`, et sans elles un appui long sur le score sélectionnerait le texte.
- **La géométrie du scoreboard est dupliquée dans deux tokens CSS, comme celle du paramétrage.** `main.css` — `--game-popup-inset-left` / `--game-popup-inset-right` (`calc(100vw * 0.4 + var(--spacing) * 4)`, en unités de grille depuis la 11.1) réservent la carte visée plus une gouttière, pour centrer la pop-up de saisie dans la zone libre. **Même dette que `--setup-popup-inset-*`** (5e passe de la 10.3), et surtout **pas la même géométrie** : le scoreboard n'a **pas** de barre latérale et ses colonnes valent 2/5 · 1/5 · 2/5, contre sidebar `--size-sidebar` (15 unités) + colonne à 1/4 pour l'étape `players`. Les deux paires doivent bouger avec leur mise en page respective ; les confondre décentrerait la pop-up de plusieurs dizaines de pixels, ce qu'aucun test unitaire ne verrait (`calc()` sur `100vw`, happy-dom ne calcule rien). La vraie solution (mesurer la carte au runtime, ou passer par `@container`) reste disproportionnée pour deux écrans — à revoir si un troisième apparaît.
- **`NumericPad` et le style des pop-ups restent à aligner en Story 10.7.** `ScoreEntryDock` prend la coquille de la 10.3 (carte `--radius-modal`, touches `--radius-key`, `ANNULER`/`VALIDER` en pied), donc la pop-up de saisie du scoreboard est désormais cohérente avec celles du paramétrage. Restent la sémantique de dialogue et la garde `prefers-reduced-motion` (DT3), la passe contraste AA, et les plafonds de `clamp()` trop bas pour le 21,5″ — à 1920×1080 le score à un chiffre est plafonné à 320 px par la borne pixel de `SCORE_SIZE_CLASSES`.
- **L'anneau du chrono recouvre le liseré de tour** sur sa largeur de débordement (~15 px). La carte porte `@container`, donc `contain: layout`, donc un **contexte d'empilement** qui confine le `z-20` de l'overlay de liseré ; l'anneau, `z-10` dans le contexte racine, passe devant. Le débordement étant un premier jet que Nathan ajuste au rendu (décision 4), le défaut est **laissé en l'état** : si le débordement est conservé, sortir le liseré de la carte (overlay frère, au niveau de `GameView`) ou abaisser l'anneau règle le cas.
- **Cinq assertions de `GameView.test.ts` portent sur un `data-testid` qui n'existe nulle part.** `expect(wrapper.find('[data-testid="prompt-close"]').exists()).toBe(false)` — `PromptModal` n'a jamais émis ce testid, la croix ayant été retirée avant son introduction : ces cas ne peuvent pas échouer. Préexistant à la 10.4 et hors de son périmètre (une sixième occurrence, héritée de `modal-close-button`, a été alignée dessus par cohérence). À remplacer par une assertion vraie — par exemple le nombre de boutons de la pop-up — à la **revue de fin d'Epic 10**.
- **Revue de code de la 10.4 reportée**, comme la 10.1, la 10.2 et la 10.3 : livrée, committée, laissée en `review` sans relecture (décision de Nathan, 2026-09-11 — revues et correctifs groupés en fin d'Epic 10).

## Deferred from: 1re passe de rendu de la story 10.4 (2026-09-14)

- **`pass-turn` et `refresh` partagent le même tracé.** `PictoIcon.vue` — la boucle circulaire à deux flèches de Billiboard sert PASSER LE TOUR (scoreboard) et CHANGER DE BILLE (paramétrage). Les deux écrans ne se croisent jamais, et les entrées restent distinctes pour pouvoir diverger, mais `PictoIcon.test.ts` a dû assouplir son cas « une forme différente par nom » en un couple documenté. À revoir si un troisième écran affiche les deux ensemble.
- ~~**L'anneau du chrono recouvre le liseré de tour, et plus visiblement qu'avant**~~ **✅ Clos (2e passe de rendu, 2026-09-14)** : `@container` est descendu de la racine de `PlayerPanel` sur un wrapper intérieur, la racine ne crée donc plus de contexte d'empilement et le `z-20` du liseré bat le `z-10` de l'anneau. Le liseré passe devant, l'anneau a pu grossir dans la foulée. Texte d'origine : : le disque a grandi en même temps que l'arc rentrait. Même cause qu'à la livraison (`@container` = contexte d'empilement qui confine le `z-20` du liseré), et Nathan a demandé le débordement en connaissance de cause (« fais déjà ça et j'adapterai »). Toujours ouvert.
- **Le bandeau garde de l'espace libre sous ses deux lignes.** Sa hauteur est fixée à 22 % de la carte (AC1) alors que son contenu n'occupe plus que deux lignes serrées en haut. À arbitrer à la prochaine passe : soit réduire le bandeau, soit répartir les deux lignes sur sa hauteur.
- **Contraste de la zone de série en `--color-brand-red`** : ≈ 4,1:1 sur le jaune `#FFE000`, ≈ 5,4:1 sur le blanc. Conforme AA pour du texte large, ce qu'est ce nombre (≥ 24 px en `font-black`). À revalider dans la passe contraste AA de la **Story 10.7**.

## Deferred from: 2e passe de rendu de la story 10.4 (2026-09-14)

- **`--color-shot-clock-face` est une composition figée à la main.** `main.css` — le disque du chrono doit rendre exactement ce que rend la colonne centrale (`--color-surface` sur `--color-bg`), mais opaque, puisqu'il déborde sur des cartes claires. Aucune variable CSS ne sait composer deux couleurs : la valeur (`#1C1F25`) est donc calculée et figée. **À recalculer si `--color-surface` ou `--color-bg` bouge** — sinon le chrono recommencera à trancher sur la colonne, ce qui est précisément le défaut corrigé ici. Même famille de dette que les `--*-popup-inset-*`.
- ~~**`PlayerPanel` dépend d'un détail d'empilement CSS pour son signal le plus important.**~~ **✅ Sans objet (3e passe)** : le liseré ne passe plus devant l'anneau, il le contourne. `@container` est revenu sur la racine et le confinement est désormais VOULU. Texte d'origine : Le liseré de tour ne passe devant l'anneau que parce que la racine de la carte ne crée **aucun** contexte d'empilement : y remettre `@container`, un `z-index`, un `transform`, un `filter` ou une `opacity` < 1 le ferait repasser derrière, **sans qu'aucun test ne le voie** (happy-dom ne calcule pas le CSS). Un cas verrouille l'absence de `@container` sur la racine ; les autres propriétés ne sont pas couvertes. À garder en tête à la revue de fin d'epic.
- ~~**Le bandeau garde de l'espace libre sous ses deux lignes** (reporté de la 1re passe, toujours ouvert)~~ **✅ Clos (3e passe, 2026-09-14)** : hauteur libre, à la mesure du contenu. Texte d'origine : : sa hauteur est fixée à 22 % de la carte alors que son contenu n'occupe plus que deux lignes serrées en haut.

## Deferred from: 3e passe de rendu de la story 10.4 (2026-09-14)

- **Le prolongement du liseré de tour vit dans `ShotClock`.** C'est sémantiquement bancal — un chrono n'a pas à connaître le tour — mais lui seul connaît la géométrie du disque (`min(100cqw, 100cqh)` sur un conteneur de taille) ; le caler depuis la colonne demanderait de recalculer cette taille à la main. `CenterPanel` ne fait que relayer `turnSide`, sans s'en servir. À revoir si le chrono sort un jour de la colonne centrale.
- **`--game-clock-bleed` a deux consommateurs qui doivent coïncider au pixel** : la largeur de la colonne (`CenterPanel`) et le `clip-path` du demi-anneau (`ShotClock`). Le token est la source unique, mais le padding de la colonne (16 px) est encore écrit en dur dans le `calc()` de la largeur — si `p-2` change, la largeur suit mal. Même famille de dette que les `--*-popup-inset-*`.
- **L'AC1 fixe le bandeau à 22 % de la carte, ce qui n'est plus le cas** : hauteur libre depuis cette passe (~16 % avec un nom court, ~21 % sur deux lignes). Annoté dans `epics.md` et la spec UX ; à reprendre si un jour la carte doit garder une grille fixe entre les deux joueurs — aujourd'hui, deux noms de longueurs différentes donnent deux bandeaux de hauteurs différentes, ce que Nathan n'a pas relevé mais qui se verra sur un nom très long face à un nom court.

- **Le détourage du liseré autour du chrono repose sur deux z-index explicites** (`z-10` sur le liseré de `PlayerPanel`, `z-20` sur le débordement de `CenterPanel`) et sur un clip calé au même `--game-clock-bleed` que la largeur de la colonne. Trois valeurs dans trois fichiers doivent rester cohérentes pour que le raccord reste net, et **aucun test unitaire ne peut le voir** (happy-dom ne calcule pas le CSS) : les cas verrouillent les classes, pas le rendu. Le contexte d'empilement de `@container` avait d'abord été jugé suffisant — il ne l'était pas, le liseré droit restant visible en travers du disque. À vérifier au navigateur à chaque retouche du chrono ou de la carte.
- **La colonne centrale n'a plus de contour**, contrairement à l'AC5 et à UX-DR48 (annotés). Si la direction visuelle de l'epic veut y revenir, il faudra un filet qui s'arrête proprement au bord du disque — ou renoncer au débordement du chrono.
- **Le commentaire de tête de `CenterPanel` a été écrit une fois À LA RACINE DU GABARIT**, ce qui en a fait un fragment et lui a fait perdre `classes()` : deux cas sont tombés d'un coup. Piège déjà payé en 10.1 sur `ModeTile` et rappelé dans les Dev Notes de cette story — il se represente dès qu'on documente une classe au plus près d'elle. Le rappel est désormais dans le fichier lui-même.

## Deferred from: 4e passe de rendu de la story 10.4 (2026-09-14)

- **`npm run build` attrape ce que `vue-tsc --noEmit` laisse passer.** `CenterPanel` — remplacer le `v-if="secondsRemaining !== null"` du chrono par un `v-if` sur un `computed` équivalent fait perdre le narrowing `number | null` → `number` que `ShotClock` exige : `--noEmit` reste vert, `vue-tsc -b` (le build) refuse. La commande de validation de référence est donc **`npm run build`**, pas `npx vue-tsc --noEmit`. À consigner dans CLAUDE.md §9 à la Story 10.7.
- **L'AC2 de la 10.4 est caduc** : `POUR n` ne double plus le restant, il le remplace dans le bandeau. Annoté dans `epics.md` et la spec UX. Rien à faire, sauf si une story ultérieure voulait réafficher le restant nu pendant l'annonce — ce que Nathan a explicitement écarté.
- **La zone de série du pied n'a plus que deux contenus possibles** (valeur en cours de frappe, série ouverte) alors que l'AC3 en décrit quatre. La table `SLOT_TESTIDS` et le `computed` de priorité restent extensibles, mais l'AC n'est plus à jour — à reprendre à la revue de fin d'epic si un troisième cas réapparaît.

## Deferred from: 5e passe de rendu de la story 10.4 (2026-09-14)

- **Le chrono est 10 px sous le centre des scores en 3 Bandes.** `CenterPanel` — il reste en `flex-1` entre le compteur (haut) et `PASSER LE TOUR` (bas), qui ne sont pas symétriques : son centre tombe à 319 px contre 309 px pour les scores. Le compteur, lui, a été sorti du flux et centré sur la colonne entière (309/311). Non corrigé : l'écart est imperceptible sur un disque de 273 px, et le chrono tire sa TAILLE de ce `flex-1` (`min(100cqw, 100cqh)`) — le sortir du flux obligerait à lui donner une hauteur explicite, et toute compensation en dur se décalerait au premier changement du compteur ou du CTA. À revoir si Nathan le relève au rendu.
- **Les deux cartes peuvent avoir des bandeaux de hauteurs différentes**, donc des scores qui ne s'alignent pas entre eux : mesuré à 323 px contre 309 px avec un nom sur deux lignes face à un nom court. Conséquence du bandeau à hauteur libre (3e passe). Déjà noté, toujours ouvert.

## Deferred from: 3e passe de rendu de la story 10.5 (2026-09-14)

- ~~**Contraste du texte sur le ruban de victoire, à trancher à la passe AA (Story 10.7).**~~ **CLOSE le 2026-09-14 par la Story 10.7**, et close PAR LA COULEUR : `--color-victory-ribbon` est passé de `#E63946` à `#D0343F` (décision de Nathan), ce qui porte le blanc de 4,17:1 à **4,95:1** — conforme même au seuil 4,5:1 du texte courant, donc la marge ne dépend plus de la taille des valeurs. Le noir tombe à 4,24:1 : le blanc est désormais le plus contrasté des deux, et le choix de rendu de la 10.5 n'a plus de contrepartie. Texte d'origine conservé ci-dessous pour mémoire. Reste ouvert, inchangé : le design system des couleurs (entrée suivante).
- *(pour mémoire, état d'avant la correction)* **Contraste du texte sur le ruban de victoire, à trancher à la passe AA (Story 10.7).** Le texte de la colonne victorieuse est passé en **blanc** à la demande de Nathan (cohérence : tout le reste de l'app est en blanc, colonne perdante voisine comprise). Sur `--color-victory-ribbon` (#E63946), le blanc donne **4,17:1** et le noir **5,04:1** — le choix retenu est donc le moins contrasté des deux. Il passe AA ici parce que **toutes** les valeurs du récap sont en `text-label` (≥ 24 px, gras) et relèvent du « grand texte » (seuil 3:1), mais il ne passe pas le seuil 4,5:1 du texte courant. **Deux conséquences** : ne pas réduire la taille de ces valeurs sans revoir la teinte ; et si le seuil 4,5 est visé, assombrir le ruban vers `--color-brand-red` (#D0343F) porterait le blanc à 4,95:1. Chiffres consignés au-dessus du token dans `main.css`.
- **Design system des couleurs à écrire (Nathan, 2026-09-14).** Le sujet des deux rouges voisins (`--color-brand-red` de la sidebar, `--color-victory-ribbon` de la colonne gagnante) est **clos sans changement** : « ça ne me choque pas ». Nathan veut à la place un **design system listant les couleurs utilisables et leurs usages**, à faire plus tard — hors Epic 10, et à ne pas improviser story par story d'ici là.

## Deferred from: dev-story 10-7-finition-transverse-semantique-des-pop-ups-reduced-motion-contraste-et-portrait (2026-09-14)

- **L'accroche de tuile (`ModeTile`, prop `tagline`) ne tient pas AA à la taille où elle est écrite.** Elle est passée de `text-white/80` (**2,78:1** sur le stop clair de `--gradient-blue`) à `text-white` (**3,46:1**), ce qui reste **sous le seuil 4,5:1** qui s'applique à `text-stat` — « grand texte » seulement au-delà de 1555 px de large (14 px à 1133, 23 px à 1920 depuis la 11.1). Non bloquant aujourd'hui : **aucune tuile n'affiche d'accroche** (décision de Nathan en 10.1, verrouillée par `HomeScreen.test.ts`), et la prop est volontairement gardée au contrat. ⚠️ **Si les accroches sont un jour rouvertes, la taille doit monter en même temps** — `text-label` (≥ 22,66 px en `font-black`, donc « grand texte », seuil 3:1) conviendrait, ou un fond plus sombre sous l'accroche.
- **Les deux tokens rouges valent désormais la même couleur** (`--color-brand-red` et `--color-victory-ribbon` à `#D0343F`) et restent **deux tokens distincts**, par décision explicite de Nathan : deux rôles qui peuvent rediverger, et un design system des couleurs à écrire plus tard (entrée de la 3e passe de la 10.5, toujours ouverte). Un cas de `main.css.test.ts` verrouille la non-fusion. À reprendre quand le design system arrivera : c'est lui qui dira si les deux rôles se rejoignent pour de bon.
- **La garde `prefers-reduced-motion` cite les deux classes à couper, nommément.** Toute nouvelle animation d'**ornement** doit venir s'y ajouter à la main : il n'y a volontairement **pas** de garde attrape-tout, qui emporterait le fondu du chrono et la barre de rebours — les deux seules animations porteuses d'information. La règle est écrite dans `CLAUDE.md` §11, mais rien ne l'applique automatiquement : c'est une discipline de revue.
- **`main.css` est le seul CSS lu par un test, et il a fallu ouvrir `vitest.config.ts` pour ça.** Vitest remplace par défaut tout import CSS par une chaîne vide, **`?raw` compris** — le motif `import source from './x.vue?raw'` employé partout ailleurs rendait ici `''`, et le test passait en croyant vérifier quelque chose. `test.css: { include: [/main\.css/] }` le laisse passer, lui seul. À garder en tête si un autre fichier CSS devait un jour être lu par un test.
- **Les touches du clavier alphabétique sont sous le plancher de 57 px EN LARGEUR sur les deux formats iPad.** Mesuré à la passe navigateur de la 10.7 : **47×57 px à 1133×744**, **51×57 px à 1194×834**, **82×93 px à 1920×1080** (65×57 avant la grille fluide de la 11.1). La hauteur tient le plancher partout ; seule la largeur manque, de 10 px au format le plus étroit. **Antérieur à cette story** (clavier livré en 10.3), **non corrigé ici car ce n'est pas « une retouche »** : la largeur d'une touche est le quotient de la largeur de la pop-up par 10 colonnes, et cette largeur est fixée par `--game-popup-inset-left`/`-right` (`calc(100vw * 0.4 + var(--spacing) * 4)`), c'est-à-dire par la décision de rendu de Nathan du 2026-09-12 — la pop-up se loge dans la zone libre, **du côté opposé à la carte visée, qui doit rester visible**. Élargir les touches, c'est faire passer la pop-up par-dessus la carte. Trois pistes si le sujet est rouvert : réduire à 9 colonnes, réduire le `gap-1` (une unité de grille, 8 px sur tablette) entre touches, ou revoir l'inset. ⚠️ Le seuil lui-même est une **exception déjà assumée** (UX-DR8 / spec §10.6 : « touches ≈ 57 px, comme le clavier natif de l'iPad ») — la règle des 90×90 px ne s'applique pas aux claviers, et toutes les commandes de jeu, elles, la tiennent aux trois formats (vérifié, aucune sous le seuil).
- **Le test de la garde verrouille le TEXTE de la règle, pas son effet.** happy-dom ne calcule aucun CSS : seul le navigateur peut dire que le voile de flash a bien disparu sous `reduce`. Vaut pour toute règle `@media` qu'on voudrait tester plus tard.

## Deferred from: code review de l'Epic 10 (stories 10.1 à 10.7) (2026-09-15)

- **Rechargement PWA en plein paramétrage.** `usePwaUpdate` applique une mise à jour dès `status === 'idle'` — or l'étape joueurs de `HomeScreen` est encore `idle` : noms et distances tapés sont perdus au rechargement. Antérieur (1.13). Piste : exposer un `setupInProgress` (ou passer le paramétrage sous un statut de store) et l'ajouter à la garde du `watch`.
- **Auto-validation 3 s après `REPRENDRE`.** `ScoreEntryDock` (comme `ScoreEntryModal` avant lui) arme le timer en `immediate` : une partie reprise avec `entryOpen` et un buffer non vide se valide seule 3 s après le retour, sans frappe. Antérieur (1.5/1.12). À trancher : c'est peut-être voulu (la saisie reprend là où elle en était), sinon ignorer le premier passage du `watch` (`old === undefined`).
- **Deux joueurs à la distance → joueur 2 seul vainqueur.** Si le joueur 1 est amené à sa distance par un `+` HORS de son tour (ajustement, aucune détection), puis que le joueur 2 atteint la sienne par une série, `checkEndOfGame` déclare `player2` vainqueur alors que les deux y sont. Antérieur (1.10), cas d'arbitrage rare.
- **Mécanisme de fermeture au voile dupliqué quatre fois.** `armBackdropClose`/`disarmBackdropClose`/`closeFromBackdrop` vivent à l'identique dans `ScoreEntryDock`, `NumericPadDock`, `AlphaKeyboardSheet` et `PromptModal` ; `ALIGN_CLASSES` est en double (`NumericPadDock`/`AlphaKeyboardSheet`), `BALL_PICTOS` aussi (`GameSummary`/`PlayerSetupCard`). Un composable `useBackdropClose()` et un module de constantes : la prochaine correction (ex. `pointercancel`) sera sinon oubliée dans l'une des copies. Refactor, pas un bug.
- **Demi-anneau de tour du chrono : géométrie supposée, jamais garantie.** `ShotClock` clippe une bande fixe de `--game-clock-bleed` (24 px) au bord du disque en supposant qu'il déborde d'exactement 24 px dans la carte ; or le disque vaut `min(100cqw, 100cqh)` — dès que la hauteur contraint (iPad paysage avec barre Safari, ~1180×673, le format réel), il peut être plus petit que la colonne élargie, ne plus déborder, et le demi-anneau rouge se peint alors en pleine colonne, déconnecté du liseré. Trois valeurs couplées sans vérification : le clip de `ShotClock`, le `-mx-[calc(bleed+16px)]` de `CenterPanel` (qui recopie le `p-2` de la colonne) et la gouttière `pr-4`/`pl-4` de `PlayerPanel`. À vérifier au navigateur à ~1180×673 avant de conclure.
- **Noms longs : le responsive des libellés de nom est à surveiller (Nathan, 2026-09-15).** Aujourd'hui les noms sont courts (« JOUEUR 1 », un prénom). En tournoi officiel, le nom affiché sera au moins l'initiale du prénom et le nom de famille entier — exemple réel : *Cédric Melnytschenko* → `C. MELNYTSCHENKO`, 16 caractères, et il en existe de plus longs. À vérifier partout où un nom s'écrit : bandeau de la carte joueur (`line-clamp-2`), bandeau du récap (`truncate`, DT6), titres de pop-up (« C. MELNYTSCHENKO A FINI » — le titre a déjà été raccourci de « a atteint sa distance » pour ça), CTA « C. MELNYTSCHENKO JOUE », cartes de paramétrage. Une troncature qui coupe un nom de famille est acceptable, une qui coupe une valeur ne l'est pas. À traiter avec les comptes joueurs (Epic 4), quand le format du nom affiché sera fixé.

## Deferred from: code review de la story 11.1 (2026-09-16)

- **Biais du bandeau de récap à 32 px fixes dans une grille fluide.** `GameSummary.vue` — les deux `clip-path` du bandeau (`calc(100%-32px)`, `32px_100%`) sont restés en pixels alors que `pl-10`/`pr-10` et `text-title` suivent la grille (×1,63 à 1920) : la coupe en biais, signature du design system, s'aplatit sur le grand écran. Mise en page hors périmètre de la 11.1 ; à reprendre en 11.3 avec les composants, en unités de `--spacing` (`calc(100% - var(--spacing) * 4)`), à valider au rendu 1920.
- **Deux syntaxes Tailwind pour lire un token de taille.** Treize usages antérieurs (et leurs tests) écrivent `min-h-[var(--size-touch-target)]`, la 11.1 introduit la forme courte de Tailwind 4 `min-h-(--size-start-button)` / `min-h-(--size-key-numeric)` / `min-h-(--size-field-min)` / `min-h-(--size-tile-min)`. Même CSS émis, deux graphies ; balayage mécanique à faire en une fois (gabarits + assertions de `IconAction.test.ts`, `PromptModal.test.ts`) vers la forme courte, hors de toute story de rendu.

## Deferred from: dev-story 11-2-palette-rayons-et-tokens-arbitres (2026-09-16)

- **Anneau de focus sur une tuile ou un CTA bleu roi : 1,82:1.** L'anneau `focus-visible` est passé au rouge vif (`--color-turn-active`, décision de Nathan) ; sur le marine il tient 3,6 à 4,2:1, mais sur le bleu roi (`#1D4ED8`, passe de fin) il fait 1,82:1 sur le stop sombre et 1,00:1 sur le clair `#3B82F6` (seuil 3:1 des composants ; le blanc y ferait 6,70:1 — sur le bleu drap `#0573BB` de la première livraison c'était 1,37:1). Sans clavier sur la borne, l'anneau n'apparaît jamais en usage (arbitrage « borne fixe »). À rouvrir seulement si une navigation clavier arrive (arbitre déporté, Epic 7).
- **Cartes joueur arrondies et séparées par une marge, barre basse « mieux découpée » (Nathan, photo Billiboard, 2026-09-16).** Sur la référence, la carte jaune et la barre `+1` ont un léger arrondi et une marge noire entre elles. Nathan n'a pas voulu l'ombre de barre basse (« pas hyper bien intégrée »), et a pointé cette découpe comme la vraie réponse. Mise en page → **Story 11.3** ; à essayer au rendu avec les composants communs (carte de pop-up, CTA), car la décision « conteneurs à angles vifs » de `DESIGN.md` › Shapes en serait l'exception.
- **Verre des pop-ups : le jaune de la carte transparaît sous la carte.** Voulu (flou 8 px à 88 % validé par Nathan sur trois niveaux d'intensité), mais à revoir si la 11.3 change la géométrie des pop-ups : une carte de pop-up posée sur une carte joueur jaune tire vers l'olive. `-webkit-backdrop-filter` n'est pas préfixé par Tailwind : à vérifier sous WebKit avec le reste de l'Epic 10 (contrôle WebKit toujours ouvert).

## Deferred from: code review de la story 11.2 (2026-09-16)

- **`tokens.test.ts` recopie les listes de DESIGN.md à la main.** `RADIUS_ROLES` / `SHADOW_ROLES` (`1score/src/assets/tokens.test.ts:14-17`) sont recopiés du frontmatter, jamais lus : DESIGN.md peut renommer `popup` ou ajouter une ombre sans qu'aucun test ne bouge, et le « miroir dans les deux sens » n'est que la comparaison de deux listes écrites par la même main. Même patron que `typography.test.ts`, accepté en 11.1. À faire lire le frontmatter YAML par le garde-fou outillé de la 11.4.
- **Harnais `render-static.cjs` : parsing d'arguments fragile.** Pas d'`outDir` → `fs.mkdirSync(undefined)` ; `--url` ou `--override` en dernier argument → valeur `undefined` (ou le drapeau suivant pris pour valeur) ; fichier de surcharge absent → `ENOENT` hors du `catch` ; format sans `x` → viewport `NaN` ; `audit.json` jamais écrit si un format plante (pas de `finally`) ; chemin nvm personnel en dur en tête du script ; seuil de cible fixe à 90 px. Outil de dev hors build, non testé, utilisé par une seule personne : à durcir si un second poste ou la 11.4 s'en sert.
- **`PASSER LE TOUR` : contraste du libellé `stat` mesuré en un point.** DESIGN.md › Buttons annonce 4,95:1 « au milieu du dégradé » ; sur le stop clair `#3B82F6` le blanc fait 3,68:1, et le libellé `text-stat` (14 px gras sur tablette) peut s'étaler vers le haut du bouton sous le picto. Mesurer sur la surface réelle (Do's de DESIGN.md) aux scans de contraste de la 11.4 ; si ça ne tient pas, monter le libellé en `label` comme les CTA de réglage l'ont fait en 11.1.

## Deferred from: dev-story 11-3-bibliotheque-de-base-cta-pop-up-composable-et-constantes-partagees (2026-09-16)

- **Découpe Billiboard : reportée en story de mise en page (décision de Nathan, au rendu).** Deux propositions ont été rendues aux trois formats par surcharges CSS, sans toucher au code : **B1** (gouttière d'une unité de grille entre les trois colonnes, avec les bords et avec la barre basse, rayon tapable sur chaque bloc, surface voilée sous la colonne centrale) et **B2** (zone de jeu soudée mais décollée des bords et de la barre). **Les deux cassent le chrono** : le disque du 3 Bandes déborde de `--game-clock-bleed` (3 unités) sur les deux cartes — c'est sa signature —, et toute gouttière le coupe net, le cercle devient un cercle tronqué. Le remettre d'aplomb demande de **recalculer le débordement contre la nouvelle gouttière** (`--game-clock-bleed` lu par `ShotClock`, `CenterPanel` et `PlayerPanel` ; plus le liseré de tour `ring-8`), donc du markup et non une surcharge : hors périmètre d'une story de refactor. `DESIGN.md` › Shapes reste **inchangé** — l'exception à « conteneurs à angles vifs » n'est pas prise. À rouvrir en story de mise en page dédiée, en traitant la géométrie couplée du chrono d'abord.
- **Touches alpha sous le plancher UX-DR8 : exception confirmée (décision de Nathan, 2026-09-16).** Re-mesuré au navigateur : **47 px** à 1133×744, **50 px** à 1180×733, **82 px** à 1920×1080, pour un plancher UX-DR8 de 57 px. Les quatre leviers ont été chiffrés sur 1133, le seul format qui ne tient pas d'assez loin pour compter : 9 colonnes → 53,5 px, `gap-0.5` → 50,9 px, gouttière extérieure supprimée → 50,5 px, 8 colonnes → 61,1 px. **Seul le passage à 8 colonnes atteint le plancher, et il défigure l'AZERTY.** Le seuil est déjà une exception assumée (UX-DR8 / spec §10.6, « touches ≈ 57 px, comme le clavier natif de l'iPad ») et la règle des 90×90 ne s'applique pas aux claviers : l'écart est **confirmé et consigné**, pas corrigé. À rouvrir seulement si une plainte d'usage remonte du club.
- **Passe de rendu V1.2 : formats de CTA, reliefs, et la découpe du scoreboard — UNE grande story à part (décision de Nathan, 2026-09-17).** Nathan veut revoir « des formats de CTA, des reliefs etc. », du même ordre que la découpe du scoreboard ci-dessus : ce sont des décisions de RENDU, pas du refactor, et elles se rassemblent. Ce que la Story 11.3 a rendu bon marché, et qui fixe le périmètre technique : un format de variante de CTA se change en **un seul endroit** (la table `VARIANT_CLASSES` de `CtaButton.vue`, six variantes) ; un relief, un rayon, une taille ou un rôle typographique se changent en **deux**, et dans cet ordre imposé (`CLAUDE.md` §7) — frontmatter de `DESIGN.md` (`shadows:` / `rounded:` / `sizes:` / `typography:`) **puis** `main.css`, jamais dans un gabarit. `tokens.test.ts` et `typography.test.ts` tiennent le miroir dans les deux sens et refusent toute valeur arbitraire : la story ne peut pas déraper en silence. **Méthode** : passes de rendu par surcharges CSS aux trois formats (rodée en 11.2 et 11.3), Nathan tranche au rendu, puis `DESIGN.md` d'abord et le code ensuite. ⚠️ Deux points durs connus : la **géométrie couplée du chrono** (l'entrée ci-dessus — c'est elle qui bloque la découpe, et il faut la traiter avant toute gouttière), et le fait que certains reliefs **rouvrent des arbitrages de la 11.2** (l'ombre de barre basse, écartée par Nathan : « pas hyper bien intégrée »). ⚠️ **Séquencement à trancher** : placée AVANT la 11.4, le ré-audit de la 11.4 mesure le design final ; placée APRÈS, la 11.4 fournit ses scans outillés pour la piloter mais son audit est périmé le jour même.
- **Harnais `render-static.cjs` : chemin Playwright en dur, corrigé à moitié.** Le chemin absolu vers le module Playwright ne résolvait sur aucune machine mais celle d'origine ; il est désormais surchargeable par `PLAYWRIGHT_MODULE` (le défaut est inchangé). Playwright reste une dépendance **empruntée**, hors `package.json` : il faut une installation compatible avec le build de Chromium présent dans `~/Library/Caches/ms-playwright` (1.58 ↔ chromium 1208 au 2026-09-16). Reste dans le lot « durcir le harnais » de la revue 11.2.
- **La CSS du serveur de dev accumule des utilitaires morts.** Après le retrait des `--*-popup-inset-*`, `pr-[var(--game-popup-inset-right)]` était **toujours émis** par le serveur `npm run dev` lancé avant le refactor : le cache Tailwind de Vite ne réélague pas ce qu'il a déjà généré. Sans conséquence de rendu (aucun gabarit ne les nomme plus, et le build de production est propre), mais **toute vérification de CSS faite sur un serveur de dev de longue vie est à refaire après redémarrage** — la comparaison pixel de la 11.3 a été rejouée sur serveur neuf pour cette raison. À dire dans la méthode des passes de rendu.
