# Deferred Work

## Deferred from: code review of story-1.1 (2026-09-08)

- Durcir la gestion d'erreurs de `PWABadge.vue` (fetch/`r.update()` sans `.catch()`, statut HTTP non-200 ignoré, `setInterval` jamais nettoyé au démontage, pas de callback `onRegisterError`, listener `statechange` non retiré après activation) — composant généré tel quel par le starter PWA, hors périmètre de la story 1.1. [carom-scoreboard/src/components/PWABadge.vue] **✅ Supprimé en Story 1.13 (2026-09-10)** : remplacé par `composables/usePwaUpdate.ts` (`registration.update()` sans `fetch` préalable, erreurs absorbées en `console.error('[pwa] …')`, `onRegisterError`, intervalle nettoyé par `onScopeDispose`, application différée à l'accueil).
- Compléter `workbox.globPatterns` (polices `woff`/`woff2`) et fournir un `navigateFallback` de production, une fois le routing implémenté (actuellement seul `devOptions.navigateFallback` est défini et `devOptions.enabled` est `false`). [carom-scoreboard/vite.config.ts] **✅ Traité en Story 1.13 (2026-09-10)** : `globPatterns` étendu à `woff2`, `navigateFallback: 'index.html'` explicite dans `workbox`.
- Résoudre l'incohérence `<html lang="en">` vs. contenu/commentaires en français, une fois le contenu réel de l'application introduit. [carom-scoreboard/index.html] **✅ Traité en Story 1.13 (2026-09-10)** : `<html lang="fr">`, titre « Carom Scoreboard », manifest `lang: 'fr'`.
- Ajouter un outillage lint/format (ESLint/Prettier) pour faire respecter mécaniquement les règles TypeScript strictes déjà activées. [carom-scoreboard/]

## Deferred from: code review of 1-2-redaction-de-claude-md (2026-09-08)

- Formule « élimine le délai 300ms iPad/Android » (AR8) potentiellement obsolète sur navigateurs modernes — héritée telle quelle de la formulation source `architecture.md`, à corriger au niveau de la source. [carom-scoreboard/CLAUDE.md:35]
- Breakpoint `lg:` documenté à ≥1280px ne correspond pas à la valeur par défaut Tailwind (1024px = `lg`, 1280px = `xl` par défaut) et aucun breakpoint explicite ne couvre la plage 768–1280px — hérité tel quel d'AR19 dans `architecture.md`. [carom-scoreboard/CLAUDE.md:112-119]
- L'exemple d'emit `update:score` (convention « kebab-case », AR15) contient un `:` non kebab-case — incohérence héritée de la formulation source. [carom-scoreboard/CLAUDE.md:30]
- Plusieurs règles (AR12 storage, AR17 mutations, AR18 async, AR16 tests, AR8 « élément critique ») sont formulées avec un périmètre strict laissant des zones grises pour les composables, les futurs fichiers `*Service.ts`, ou les tests de store/service — ambiguïtés héritées de la formulation exacte d'`architecture.md`, à traiter lors d'une story future touchant concrètement ces couches. [carom-scoreboard/CLAUDE.md:16-119]
- Aucune convention documentée pour l'identifiant de store Pinia (`defineStore('id', ...)`) ni pour les fichiers utilitaires génériques non préfixés `use` — absents du tableau de nommage source (AR15). [carom-scoreboard/CLAUDE.md:16-31]

## Deferred from: code review of 1-3-selectionner-un-mode-jds-et-demarrer-une-partie (2026-09-08)

- **`status === 'finished'` sans branche de rendu** — `GameView.vue:25-49` ne couvre que `idle` et `playing`, sans `v-else`. Dès qu'un code posera `'finished'`, l'écran sera noir et sans issue (l'ActionBar QUITTER est à l'intérieur de la branche `playing`). Vérifié : `body.innerText` vide. À traiter par la Story 1.10/1.11. **✅ Traité en Story 1.10 (2026-09-10)** : branche `finished` rendue par `GameSummary` + barre à deux CTA (`FIN DE PARTIE`, `UNE PARTIE DE PLUS`).
- **`reprises` en `shallowRef` : les `push` futurs seront non réactifs** — `useGameStore.ts:11`. Trois `computed` de `GameView` dépendent de `reprises.value.length` ; un `shallowRef` ne notifie que sur remplacement de `.value`. Un `reprises.value.push(...)` laissera le compteur REPRISE bloqué sur 1, le bouton ANNULER grisé et l'interversion visible en pleine partie. Indétectable aujourd'hui car seules des affectations existent. AR9 impose `shallowRef` : la convention « toujours réassigner, jamais muter » doit être gravée dans CLAUDE.md avant la Story 1.5/1.8.
- **QUITTER destructif sans confirmation** — `GameView.vue:48` déclenche `resetGame()` sur `@pointerdown`, qui se déclenche au contact et ne peut pas être annulé en glissant le doigt hors du bouton. Note de périmètre déjà présente dans `epics.md:584` — Story 1.15.
- **Branche « catégorie à mode unique » = code mort non testé** — `HomeScreen.vue:38-43`. Les deux seules catégories mono-mode (`3bandes`, `casin`) sont `available: false`, donc bloquées par la garde en amont. Deviendra atteignable en Story 2.1.
- **Robustesse de la normalisation des noms de joueurs** — `HomeScreen.vue:62`. `slice(0,20)` coupe les paires de substitution (un emoji en position 20 devient un surrogate orphelin, chaîne invalide en UTF-8 — impactera la persistance Story 1.12 et la base joueurs Epic 4) ; `toUpperCase()` peut allonger la chaîne (`ß` → `SS`), faisant retronquer et perdre un caractère réel. Découper sur `[...value]` avant la mise en majuscule. *Mise à jour (2026-09-10, Story 1.12)* : **inoffensif pour la persistance** — `JSON.stringify` échappe les surrogates orphelins (`\uD83D`) depuis ES2019 (well-formed stringify), l'aller-retour `localStorage` est sans perte. Reste ouvert pour la troncature/`toUpperCase` elle-même.
- **`Player.id` : champ positionnel redondant** — `types/game.ts:22`. Vaut toujours le nom du champ qui le contient, ce que `swapPlayers` doit restamper manuellement à chaque échange ; risque d'oubli sur les futures mutations (undo, restauration de sauvegarde).
- **Ordre des classes Tailwind non conforme à CLAUDE.md §7** — plusieurs occurrences (`HomeScreen.vue:97,116`, `CenterPanel.vue:28`). Aucun outillage lint/format n'existe encore dans le projet pour le détecter (déféré depuis la Story 1.1).

## Deferred from: code review of 1-4-configurer-les-parametres-du-match-avant-de-demarrer (2026-09-09)

- **Le plafond de distance est défini deux fois, dans deux unités différentes.** `MAX_DIGITS = 3` dans `PlayerSetupModal.vue:9` et `MAX_TARGET_SCORE = 999` dans `useGameStore.ts:8` ne s'accordent aujourd'hui que par coïncidence numérique. La normalisation est bien placée dans l'action (AR17), mais rien ne fait redescendre la borne vers l'UI : si la règle produit passe à 500, la modale continuera d'accepter 999, la zone joueur affichera 999 (elle lit la valeur brute non normalisée), et `startGame` reclampera silencieusement — la partie démarrerait avec une distance différente de celle affichée une seconde plus tôt.
- **Aucune protection contre deux joueurs de même nom.** Nommer les deux joueurs `MICHEL` (ou le joueur 1 `JOUEUR 2`, chaîne saisissable) rend les deux `PlayerPanel` identiques au libellé près ; le seul discriminant restant est la couleur de fond, alors que `swapPlayers()` échange les côtés en cours de partie. Hors périmètre de la Story 1.4 (aucun AC ne le couvre) ; à rattacher à la base joueurs de l'Epic 4 ou à une garde de démarrage.
- **`button:focus-visible` annonce un chemin d'interaction qui n'existe pas.** `main.css:83-86` dessine un contour de 4 px au `Tab`, mais tous les contrôles n'écoutent que `@pointerdown` (AR8, décision assumée en revue de la Story 1.3) : `Entrée`/`Espace` émettent `click` et aucune touche ne réagit. Pré-existant à cette story. À trancher si une cible d'accessibilité clavier / switch access apparaît.
- **Couverture du clavier alphabétique : ni trait d'union, ni apostrophe, ni `Ë Ï Î Ô Û`.** `AlphaKeyboard.vue:14-19` n'offre que l'AZERTY, une rangée de chiffres, `É È À Ç`, `ESPACE` et `⌫`. `JEAN-PIERRE`, `MARIE-CLAUDE`, `D'ARTAGNAN`, `JOËL`, `ANAÏS`, `BENOÎT`, `JÉRÔME` sont donc insaisissables, sans contournement possible puisque le clavier est la seule voie de saisie (aucun champ natif, par décision « borne fixe »). Conforme à l'AC#5 tel qu'écrit, mais l'AC#5 ne couvre pas son propre cas d'usage déclaré. **Reporté sur décision de Nathan : à revoir avec la base joueurs de l'Epic 4**, où les noms seront sélectionnés plutôt que tapés — et où ajouter des touches n'aggravera plus la contrainte de hauteur de la modale.

## Deferred from: code review of 1-5-saisir-le-score-dune-serie-au-pave-numerique (2026-09-09)

- **`ÉCHANGER` en pleine reprise laisse le tour au même joueur physique et orpheline la reprise entamée.** `useGameStore.ts` — `swapPlayers` permute les colonnes de `reprises` mais `activePlayer` reste attaché au côté. Après la série du blanc et avant celle du jaune, un échange redonne la main à celui qui vient de jouer ; sa série suivante ouvre une nouvelle ligne, la ligne `{null, n}` ne se complète jamais et `completedReprises` (donc le compteur REPRISE) accuse une unité de retard jusqu'à la fin de la partie. **Décision de Nathan (2026-09-09) : accepté et documenté** (Décision 15 de la story), le tour ne suit pas le joueur et l'échange n'est pas bloqué ; le rattrapage passera par `ANNULER` (Story 1.8). À réévaluer si le cas se produit en usage réel. *Précision (Story 1.7, 2026-09-09)* : l'échange lui-même **n'est pas annulable** (décision produit, `ÉCHANGER` est son propre inverse) ; le rattrapage consiste à **annuler jusqu'avant la série orpheline**, puis à la ressaisir du bon côté.

- **Sortie destructive sur `pointerdown`, et qui change de colonne à chaque tour.** `GameView.vue:113,145` — le picto de sortie appelle `resetGame()` au contact, à l'emplacement où se trouvait `AJOUTER LES POINTS` au tour précédent. Confirmation prévue Story 1.15 ; l'alternance est la décision produit AC4 de la 1.5. **✅ Traité en Story 1.10 (2026-09-10)** : le picto ouvre une pop-up de confirmation « TERMINER LA PARTIE ? » (`PromptModal`, CTA `VOIR LE RÉCAP` / `ANNULER`) menant au récap — plus rien de destructif au contact ; sans aucune série, retour direct à l'accueil (rien à perdre). L'alternance de colonne demeure.
- **Panneau joueur entier en `role="button"` englobant deux vrais `<button>`.** `PlayerPanel.vue:100-105` — contenu interactif imbriqué interdit par ARIA, pas de `tabindex` ni d'`aria-disabled` quand le panneau est inerte ; un appui de paume sur la carte adverse enregistre une série de 0. Geste décidé par AC14 ; accessibilité clavier hors cible (revue 1.4).
- **Pop-up de saisie sans sémantique de dialogue ni `prefers-reduced-motion`.** `ScoreEntryModal.vue:120-130`, `main.css:52-79` — pas de `role="dialog"`/`aria-modal`, pas de gestion du focus, trois animations sans garde reduced-motion. Pré-existant sur `PlayerSetupModal`, même arbitrage borne fixe.
- **Voile de pop-up avec handlers pointer sans `role="button"` (CLAUDE.md §2).** `ScoreEntryModal.vue:120`, `PlayerSetupModal.vue` — la règle mérite une exception explicite pour les voiles plutôt qu'un rôle ARIA erroné sur un fond plein écran.
- **Markup dupliqué dans la barre basse.** `GameView.vue:106-176` — CTA et SVG de sortie copiés pour chaque colonne, alignement dépendant du `px-4` d'`ActionBar` via `-mx-4`/`ml-4`/`mr-4` : changer le padding de la barre désaligne silencieusement les colonnes. Nettoyage sans impact fonctionnel.
- **`MAX_TARGET_SCORE = 999` et `MAX_SCORE_DIGITS = 3` encodent FR7 indépendamment.** `useGameStore.ts:8-12` — s'ajoute au doublon déjà différé en revue 1.4 (`MAX_DIGITS` de `PlayerSetupModal`). Réconciliation hors périmètre par décision de la story 1.5.

## Deferred from: code review of 1-7-annuler-la-saisie-en-cours-avant-validation (2026-09-09)

- **`undoLastAction()` par pilotage déporté pendant la pop-up ouverte écrase le buffer en cours de frappe.** `useGameStore.ts:340-357` — l'undo restaure `currentInput`/`isNegative` sans garde ; la modale afficherait vide et l'auto-validation tomberait sur un buffer vide. Injoignable depuis l'UI (le voile plein écran recouvre `ANNULER`). À traiter avec le pilotage V2+ : `GameView` doit fermer la pop-up avant d'appeler l'undo.
- **`isNegative` n'est pas remis à zéro avant le snapshot, contrairement à `currentInput`.** `useGameStore.ts:317-318` — sans effet tant que rien ne pose le drapeau ; la Story 1.9 (saisie négative) devra vider le signe avant `pushHistory()` sous peine de le voir réapparaître après un undo (même piège que le `53` du Debug Log 1.7). *Mise à jour (2026-09-10)* : la **Story 1.9 est annulée** (FR10 couvert par les CTA `−`/`+`) ; `isNegative` reste donc inerte à `false`, sans risque. Le champ est un candidat à la **suppression** du modèle (`GameState`, `GameSnapshot`, `mirrorSnapshot`, `swapPlayers`) lors de la Story 1.12, qui fixera le format persisté. **✅ Supprimé en Story 1.12 (2026-09-10)** : `isNegative` retiré de `GameState`, `GameSnapshot`, `mirrorSnapshot`, du store et des tests.
- **Undo refusé hors `playing` : une série gagnante mal saisie serait irrattrapable une fois `status = 'finished'`.** `useGameStore.ts:341` — AC7 de la 1.7 exige le no-op hors partie et `finished` n'existe pas encore. À trancher en Story 1.11 (autoriser l'undo depuis `finished` et rouvrir la partie, ou confirmer la fin). **✅ Tranché en Story 1.10 (2026-09-10)** : le récap est **terminal**, `undoLastAction` reste un no-op hors `playing` (désormais testé avec un état `finished` réel). La correction passe par la **croix** des pop-ups « PARTIE TERMINÉE » et « TERMINER LA PARTIE ? », qui ramènent au scoreboard sans clore la partie ; la détection rejoue à la validation suivante. *Revue au rendu (Nathan, 2026-09-10)* : la croix de « PARTIE TERMINÉE » est **retirée** — on ne revient pas au scoreboard une fois la fin détectée, une série gagnante mal saisie n'est plus rattrapable, **accepté**. La pop-up de sortie n'a pas de croix non plus : son retour est un CTA `ANNULER`.
- **Le snapshot porte `name`/`targetScore` : une édition de joueur en partie serait défaite par l'undo.** `useGameStore.ts:118-127,349-350` — le commentaire d'invariante ne couvre que la mutation en place ; un REMPLACEMENT de l'objet joueur (renommage, Story 1.14) serait lui aussi annulé. À traiter en 1.14 : ne restaurer que le score, ou ré-appliquer l'édition après restauration.
- **Deux pointeurs simultanés sur `ANNULER` = deux annulations.** `CenterPanel.vue:32` — `@pointerdown` n'ignore pas les pointeurs secondaires. Cas rare ; garde `isPrimary` à centraliser dans `usePointerEvents.ts` (CLAUDE.md §2) quand ce composable existera.

## Deferred from: code review of 1-10-terminer-une-partie-et-consulter-le-recapitulatif-automatique (2026-09-10)

- **Store non durci contre les actions reçues pendant une pop-up de fin ouverte (pilotage déporté V2+).** `useGameStore.ts:399-408,444-461` — `checkEndOfGame` écrase un `endPrompt` existant ; `undoLastAction`/`adjustScore`/`swapPlayers` ne ferment pas la pop-up ; après `dismissEndPrompt` en égalisatrice, le blanc peut revalider (série plafonnée à 0, `hasReachedTarget` vrai) et déclencher une seconde offre ; `finishGame` appelé par la sortie pendant une offre vaut refus de l'égalisatrice (blanc gagnant) au lieu du prorata. Injoignable au doigt : le voile plein écran recouvre tout et `dismissEndPrompt` n'a aucun bouton. Même famille que « undo par pilotage déporté pendant la pop-up » (revue 1.7) : à traiter avec le pilotage V2+, en fermant/gelant les actions tant qu'une décision est attendue.
- **`endPrompt` absent de `GameState` alors que le store promet sa persistance en 1.12.** `types/game.ts:98-113`, `useGameStore.ts:115-118` — `GameState` reçoit `winner`/`finishedAt`/`equalizingReprise` mais pas la pop-up en attente ; une restauration en pleine offre d'égalisatrice retomberait sur un scoreboard sans pop-up, le blanc à sa distance et `equalizingReprise` faux. À fixer avec le format persisté (Story 1.12). **✅ Traité en Story 1.12 (2026-09-10)** : `endPrompt` (et `entryOpen`, `history`, `scoreAdjustments`, `sidesSwapped`) font partie de `GameState`, persistés et restaurés — testé sur l'offre d'égalisatrice et « PARTIE TERMINÉE ».
- **`PromptModal` sans sémantique de dialogue.** `PromptModal.vue:38-46` — pas de `role="dialog"`/`aria-modal`/`aria-labelledby` vers le `h2`, pas de gestion du focus. Même dette et même arbitrage « borne fixe » que `ScoreEntryModal`/`PlayerSetupModal` (revue 1.5) ; à traiter d'un bloc le jour où l'accessibilité clavier entre en cible.
- **Bandeau du récap : un nom de 20 lettres larges masque la distance.** `GameSummary.vue` (`summary-player1`/`summary-player2`, `truncate`) — « WWWWWWWWWWWWWWWWWWWW / 8 » est coupé par l'ellipse avant « / 8 » dans les deux formats tablette. Cas extrême (20 caractères les plus larges) relevé en passe visuelle de la revue 1.10 ; à traiter le jour où le bandeau est retouché (distance dans un `span` séparé non tronqué, ou nom sur deux lignes).

## Deferred from: code review of 1-12-preserver-letat-de-la-partie-en-cas-de-fermeture-accidentelle (2026-09-10)

- **Garde structurelle de `loadGameState` superficielle.** `storageService.ts:31-47` — `isPersistedGame` vérifie la version, le statut et la présence/forme des conteneurs, mais pas les valeurs internes : `mode` hors catalogue, `activePlayer` invalide, `name`/`score`/`targetScore` absents ou non numériques, cellules de `reprises` nulles, éléments de `history` malformés, `currentInput` non chaînes, `scoreAdjustments` non finis, `endPrompt`/`winner` de mauvais type. Une telle sauvegarde passe la garde et fait planter la vue au premier rendu ou au premier undo — hors du service, à rebours d'AR12. Injoignable sans manipuler le storage à la main (le store est l'unique écrivain). Décision de cadrage de la story (garde minimale, pas de Zod, filet de sécurité à développer vite). À revoir si un jour le format est lu par un autre écrivain (import, multi-appareils, Epic 3).
- **Coût par action de la pile persistée.** `useGameStore.ts:35,621` — chaque action, frappe au pavé comprise, sérialise l'intégralité de `history` (chaque snapshot recopie le tableau des reprises : ≈ 350 Ko pour 100 actions × 60 reprises), et `checkSavedGame` reparse ce blob de façon synchrone avant le premier rendu. Risque consigné en commentaire sur `MAX_UNDO_DEPTH` ; un bornage à l'écriture (`history.slice(-N)` dans `persistedState`) tient en une ligne, à poser avec le `+1` par point du 3 Bandes (Epic 2).

## Deferred from: dev-story 1-13-fonctionner-offline-et-sinstaller-comme-application-native (2026-09-10)

- **AC5 non vérifié — actions manuelles de Nathan** : rattachement du dépôt GitHub au site Netlify (AR11), puis installation et test hors ligne sur iPad et Android, et observation d'une mise à jour automatique réelle. Check-list détaillée dans [1-13-actions-manuelles-nathan.md](1-13-actions-manuelles-nathan.md). La story ne passe pas en `done` avant.
